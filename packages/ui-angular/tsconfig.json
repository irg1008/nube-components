{
  "extends": "tsconfig/angular/angular-base.json",
  "compilerOptions": {
    "baseUrl": "./src",
    "outDir": "./dist/out-tsc",
    // These do not work with ng-packagr at the moment.
    "paths": {
      "@/*": ["lib/*"]
    }
  }
}
